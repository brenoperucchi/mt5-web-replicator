.row
  .col-md-12
    .card.table-card.latest-activity-card
      .card-header
        h5
          | Account 
          '
          = "#{@account.name} - #{@account.kind}"
        .card-header-right
      .card-body
        .table-responsive
          table.table.table-hover.table-borderless
            - type = @account.slave? ? :slaves : :transactions
            thead
              tr
                / - if @account.copy?
                /   th Ticket
                / - else
                /   th Ticket Master
                /   th Ticket Slave
                th Ticket
                th Symbol
                th State
                th Profit Copy
                th Profit Slave
                th Type
                th PriceOpen Copy
                th PriceOpen Slave
                th PriceClose Copy
                th PriceClose Slave
                th Lot
                th Open Copy
                th Open Slave
                th Closet Copy
                th Closet Slave
            tbody
              tr
                td OPERATIONS
                td = @account.data_scope(type, nil, :all, @trace).count
                td TOTAL PROFIT
                td= number_with_precision @account.data_scope(:transactions, nil, :all, @trace).to_a.sum(&:profit), precision:2
                td= number_with_precision @account.data_scope(type, nil, :all, @trace).to_a.sum(&:profit), precision:2
                td - 
                td - 
                td - 
                td - 
                td -
                td -
              - @account.data_scope(type, nil, :all, @trace).order(open_at: 'DESC').each do |slave|
                / - open_at_slave = slave.loggings.where(state:["OPEN", "OPENED"]).try(:first).try(:created_at)
                tr
                  td = slave.ticket_slave
                  td = slave.symbol
                  td = slave.state
                  td = number_with_precision slave.master.profit, precision:2
                  td = number_with_precision slave.profit, precision:2
                  td = slave.ordertype
                  td = slave.master.price_open
                  td = slave.price_open
                  td = slave.master.price_closed
                  td = slave.price_closed
                  td = slave.lot.to_f.round(3)
                  td = l(slave.master.open_at, format: :short5) if slave.master.open_at.present?
                  td = l(slave.open_at, format: :short5) if slave.open_at.present?
                  td = l(slave.master.closed_at, format: :short5) if slave.master.closed_at.present?
                  td = l(slave.closed_at, format: :short5) if slave.closed_at.present?
              tr
                td OPERATIONS
                td = @account.data_scope(type, nil, :all, @trace).count
                td TOTAL PROFIT
                td= number_with_precision @account.data_scope(:transactions, nil, :all, @trace).to_a.sum(&:profit), precision: 2
                td= number_with_precision @account.data_scope(type, nil, :all, @trace).to_a.sum(&:profit), precision: 2
                td - 
                td - 
                td - 
                td - 
                td -
                td -