production:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") { Rails.application.credentials.secret_key_base } %>
development:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") { Rails.application.credentials.secret_key_base } %>
test:
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE", "test-secret-key-base-for-mt5-web-replicator") %>
